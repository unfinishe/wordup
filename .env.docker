# Production environment configuration for Docker
# Copy this file to .env.docker and customize for your deployment

# Flask secret key (IMPORTANT: Change this in production!)
SECRET_KEY=your-very-secure-secret-key-change-this-in-production

# Database configuration
# For Docker, typically use: sqlite:///data/wordup.db
DATABASE_URL=sqlite:///data/wordup.db

# Server configuration
WORDUP_HOST=0.0.0.0
WORDUP_PORT=5000

# Flask debug mode (should be false in production)
FLASK_DEBUG=false

# Reverse proxy / subpath configuration (optional)
# Set this when serving WordUp behind a reverse proxy at a subpath
# Example: APPLICATION_ROOT=/wordup for serving at https://example.com/wordup/
# Leave empty or set to "/" for serving at domain root
# APPLICATION_ROOT=/wordup

# Optional: Custom database path
# DATABASE_URL=sqlite:////var/lib/wordup/wordup.db

# Example for external database (if needed in future)
# DATABASE_URL=postgresql://user:password@db:5432/wordup